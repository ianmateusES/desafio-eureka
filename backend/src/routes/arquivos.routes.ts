import { Router } from 'express';
// import ArquivoController from '../app/controllers/ArquivoController';

const arquivosRouter = Router();

// arquivosRouter.get('/', ArquivoController.index);

export default arquivosRouter;
